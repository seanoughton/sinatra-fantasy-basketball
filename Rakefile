ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment'
#in order for the rake tasks that deal with the db to run, they need access to the db
#this access is in the environment file
require 'sinatra/activerecord/rake'



